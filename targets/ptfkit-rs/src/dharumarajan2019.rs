// @generated by ptfkit-codegen; DO NOT EDIT.

#![doc = r"Dharumarajan et al. (2019) hydraulic PTFs for the Karnataka Plateau.

# Reference

Dharumarajan, S., Hegde, R., Lalitha, M., Kalaiselvi, B., & Singh, S. K. (2019). Pedotransfer
functions for predicting soil hydraulic properties in semi-arid regions of Karnataka Plateau,
India. Current Science, 116(7), 1237-1246. https://doi.org/10.18520/cs/v116/i7/1237-1246
DOI: 10.18520/cs/v116/i7/1237-1246 (https://doi.org/10.18520/cs/v116/i7/1237-1246)

# Territory

Karnataka Plateau, India

# Dataset

Field capacity and permanent wilting point were modeled from 512 soil samples in the Northern
Karnataka Plateau and 228 samples in the Southern Karnataka Plateau; infiltration was modeled
from 100 Karnataka soil observations."]

#[doc = r"Results returned by `calc_ptf_dharumarajan2019_nkp`."]
#[derive(Clone, Copy, Debug, PartialEq)]
pub struct Dharumarajan2019WaterRetentionResult {
    #[doc = r"Soil water content at -33 kPa matric potential. (%)"]
    pub field_capacity: f64,
    #[doc = r"Soil water content at -1500 kPa matric potential. (%)"]
    pub permanent_wilting_point: f64,
}
#[doc = r"Estimate field capacity and wilting point for Northern Karnataka soils.

# Arguments

  * clay: Clay content. (%)
  * sand: Sand content. (%)
  * cation_exchange_capacity: Cation exchange capacity. (C mol p+/kg)

# Returns

A [`Dharumarajan2019WaterRetentionResult`].

# Territory

Northern Karnataka Plateau

# Models

  * h(theta): Point water-retention estimates from multiple linear regression

# Notes

Prediction target: Water content at field capacity and permanent wilting point from clay, sand,
and cation exchange capacity.
The model was fitted to 512 soil-layer observations.
Reported cross-validation RMSE values are 5.25% for FC and 3.71% for PWP.

# Warnings

The paper does not state whether its water-content percentages are gravimetric or volumetric.
The source gives inconsistent profile counts and district lists for the Northern dataset; see
the scientific notes."]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_dharumarajan2019_nkp(
    clay: f64,
    sand: f64,
    cation_exchange_capacity: f64,
) -> Dharumarajan2019WaterRetentionResult {
    let field_capacity =
        13.82f64 + 0.205f64 * clay - 0.088f64 * sand + 0.316f64 * cation_exchange_capacity;
    let permanent_wilting_point =
        -5.776f64 + 0.315f64 * clay + 0.050f64 * sand + 0.271f64 * cation_exchange_capacity;
    Dharumarajan2019WaterRetentionResult {
        field_capacity,
        permanent_wilting_point,
    }
}
#[cfg(test)]
mod calc_ptf_dharumarajan2019_nkp_tests {
    use super::*;
    fn assert_in_interval(actual: f64, lower: f64, upper: f64) {
        assert!(
            actual >= lower && actual <= upper,
            "actual {actual} is outside [{lower}, {upper}]"
        );
    }
    #[test]
    fn northern_study_mean_case() {
        let result = calc_ptf_dharumarajan2019_nkp(43.2f64, 39.8f64, 33.6f64);
        assert_in_interval(
            result.field_capacity,
            29.79119999999997f64,
            29.79120000000003f64,
        );
        assert_in_interval(
            result.permanent_wilting_point,
            18.927599999999973f64,
            18.92760000000003f64,
        );
    }
}
#[doc = r"Estimate Northern Karnataka field capacity and wilting point from clay.

# Arguments

  * clay: Clay content. (%)

# Returns

A [`Dharumarajan2019WaterRetentionResult`].

# Territory

Northern Karnataka Plateau

# Models

  * h(theta): Point water-retention estimates from simple linear regression

# Notes

Prediction target: Water content at field capacity and permanent wilting point from clay content
alone.
The paper recommends this model when clay content is the only available predictor.
Reported cross-validation RMSE values are 7.05% for FC and 4.74% for PWP.

# Warnings

The paper does not state whether its water-content percentages are gravimetric or volumetric.
The source gives inconsistent profile counts and district lists for the Northern dataset; see
the scientific notes."]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_dharumarajan2019_nkp_clay(clay: f64) -> Dharumarajan2019WaterRetentionResult {
    let field_capacity = 4.968f64 + 0.586f64 * clay;
    let permanent_wilting_point = -2.443f64 + 0.500f64 * clay;
    Dharumarajan2019WaterRetentionResult {
        field_capacity,
        permanent_wilting_point,
    }
}
#[cfg(test)]
mod calc_ptf_dharumarajan2019_nkp_clay_tests {
    use super::*;
    fn assert_in_interval(actual: f64, lower: f64, upper: f64) {
        assert!(
            actual >= lower && actual <= upper,
            "actual {actual} is outside [{lower}, {upper}]"
        );
    }
    #[test]
    fn northern_study_mean_clay_case() {
        let result = calc_ptf_dharumarajan2019_nkp_clay(43.2f64);
        assert_in_interval(
            result.field_capacity,
            30.283199999999972f64,
            30.28320000000003f64,
        );
        assert_in_interval(
            result.permanent_wilting_point,
            19.15699999999997f64,
            19.15700000000003f64,
        );
    }
}
#[doc = r"Estimate field capacity and wilting point for Southern Karnataka soils.

# Arguments

  * clay: Clay content. (%)
  * sand: Sand content. (%)
  * cation_exchange_capacity: Cation exchange capacity. (C mol p+/kg)

# Returns

A [`Dharumarajan2019WaterRetentionResult`].

# Territory

Southern Karnataka Plateau

# Models

  * h(theta): Point water-retention estimates from multiple linear regression

# Notes

Prediction target: Water content at field capacity and permanent wilting point from clay, sand,
and cation exchange capacity.
The model was fitted to 228 soil samples from 43 profiles.
Reported cross-validation RMSE values are 3.05% for FC and 2.17% for PWP.

# Warnings

The paper does not state whether its water-content percentages are gravimetric or volumetric."]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_dharumarajan2019_skp(
    clay: f64,
    sand: f64,
    cation_exchange_capacity: f64,
) -> Dharumarajan2019WaterRetentionResult {
    let field_capacity =
        39.179f64 - 0.041f64 * clay - 0.371f64 * sand + 0.257f64 * cation_exchange_capacity;
    let permanent_wilting_point =
        8.227f64 + 0.168f64 * clay - 0.101f64 * sand + 0.217f64 * cation_exchange_capacity;
    Dharumarajan2019WaterRetentionResult {
        field_capacity,
        permanent_wilting_point,
    }
}
#[cfg(test)]
mod calc_ptf_dharumarajan2019_skp_tests {
    use super::*;
    fn assert_in_interval(actual: f64, lower: f64, upper: f64) {
        assert!(
            actual >= lower && actual <= upper,
            "actual {actual} is outside [{lower}, {upper}]"
        );
    }
    #[test]
    fn southern_study_mean_case() {
        let result = calc_ptf_dharumarajan2019_skp(31.5f64, 53.5f64, 14.7f64);
        assert_in_interval(
            result.field_capacity,
            21.816899999999972f64,
            21.81690000000003f64,
        );
        assert_in_interval(
            result.permanent_wilting_point,
            11.305399999999986f64,
            11.305400000000015f64,
        );
    }
}
#[doc = r"Estimate Southern Karnataka field capacity and wilting point from clay.

# Arguments

  * clay: Clay content. (%)

# Returns

A [`Dharumarajan2019WaterRetentionResult`].

# Territory

Southern Karnataka Plateau

# Models

  * h(theta): Point water-retention estimates from simple linear regression

# Notes

Prediction target: Water content at field capacity and permanent wilting point from clay content
alone.
The paper recommends this model when clay content is the only available predictor.
Reported cross-validation RMSE values are 5.39% for FC and 3.13% for PWP.

# Warnings

The paper does not state whether its water-content percentages are gravimetric or volumetric."]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_dharumarajan2019_skp_clay(clay: f64) -> Dharumarajan2019WaterRetentionResult {
    let field_capacity = 3.724f64 + 0.581f64 * clay;
    let permanent_wilting_point = -1.979f64 + 0.428f64 * clay;
    Dharumarajan2019WaterRetentionResult {
        field_capacity,
        permanent_wilting_point,
    }
}
#[cfg(test)]
mod calc_ptf_dharumarajan2019_skp_clay_tests {
    use super::*;
    fn assert_in_interval(actual: f64, lower: f64, upper: f64) {
        assert!(
            actual >= lower && actual <= upper,
            "actual {actual} is outside [{lower}, {upper}]"
        );
    }
    #[test]
    fn southern_study_mean_clay_case() {
        let result = calc_ptf_dharumarajan2019_skp_clay(31.5f64);
        assert_in_interval(
            result.field_capacity,
            22.025499999999973f64,
            22.02550000000003f64,
        );
        assert_in_interval(
            result.permanent_wilting_point,
            11.502999999999986f64,
            11.503000000000014f64,
        );
    }
}
#[doc = r"Estimate infiltration rate for Karnataka soils from texture fractions.

# Arguments

  * sand: Sand content in the Karnataka infiltration dataset. (%)
  * silt: Silt content in the Karnataka infiltration dataset. (%)
  * clay: Clay content in the Karnataka infiltration dataset. (%)

# Returns

  * infiltration_rate: Soil infiltration rate measured with a double-ring infiltrometer. (mm/h)

# Territory

Karnataka, India

# Models

  * k(h): Multiple linear regression for infiltration rate

# Notes

Prediction target: Infiltration rate from sand, silt, and clay contents.
The model was fitted to 100 Karnataka soil observations.
The reported model R-squared is 41%, and the reported RMSE is 6.71%.

# Warnings

Predictor calibration ranges are not reported for the 100-observation dataset."]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_dharumarajan2019_infiltration(sand: f64, silt: f64, clay: f64) -> f64 {
    177.55f64 - 1.47f64 * sand - 1.80f64 * clay - 1.58f64 * silt
}
#[cfg(test)]
mod calc_ptf_dharumarajan2019_infiltration_tests {
    use super::*;
    fn assert_in_interval(actual: f64, lower: f64, upper: f64) {
        assert!(
            actual >= lower && actual <= upper,
            "actual {actual} is outside [{lower}, {upper}]"
        );
    }
    #[test]
    fn balanced_texture_case() {
        let result = calc_ptf_dharumarajan2019_infiltration(50f64, 20f64, 30f64);
        assert_in_interval(result, 18.44999999999997f64, 18.450000000000028f64);
    }
}
