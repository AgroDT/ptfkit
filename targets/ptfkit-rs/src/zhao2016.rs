// @generated by ptfkit-codegen; DO NOT EDIT.

#![doc = r"Saturated hydraulic conductivity PTFs for the Loess Plateau of China.

# Reference

Zhao, C., Shao, M., Jia, X., Nasir, M., & Zhang, C. (2016). Using pedotransfer functions to
estimate soil hydraulic conductivity in the Loess Plateau of China. CATENA, 143, 1-6.
https://doi.org/10.1016/j.catena.2016.03.037
DOI: 10.1016/j.catena.2016.03.037 (https://doi.org/10.1016/j.catena.2016.03.037)

# Territory

Typical Loess Plateau of China, covering approximately 430,000 square kilometers.

# Dataset

729 samples from 243 sites, sampled in 2012 at depths of 0-10, 10-20, and 20-40 cm. Each of 400
random calibration-validation splits used 583 development records and 146 validation records
(Section 2.3.1)."]

#[doc = r"Estimate saturated hydraulic conductivity using reconstructed M2-1.

# Arguments

  * bulk_density: Soil bulk density measured on undisturbed cores. (g/cm^3)
  * silt: Silt content measured by laser diffraction using USDA taxonomy (Section 2.2.2). (%)
  * clay: Clay content measured by laser diffraction using USDA taxonomy (Section 2.2.2). (%)

# Returns

  * ks: Saturated hydraulic conductivity, measured by the constant-head method. (mm/min)

# Notes

Prediction target: Saturated hydraulic conductivity
Highest-ranked fitted PTF, ranked by descending R-squared (Section 3.3).
Uses Table 1 means and standard deviations for Jia et al. (2012) Equation 3 on both predictors
and response, as a reviewed reconstruction.

# Warnings

The publication does not explicitly confirm Table 1 statistics as the fitted scaling constants
or the application of response scaling. This reconstruction has not been verified against author
software.
Observed ranges describe the sampled soils; predictions are not clamped."]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_zhao2016_m2_1(bulk_density: f64, silt: f64, clay: f64) -> f64 {
    let bd_scaled = (bulk_density - (1.32f64 - 2.0f64 * 0.14f64)) / (4.0f64 * 0.14f64);
    let silt_scaled = (silt - (63.33f64 - 2.0f64 * 9.85f64)) / (4.0f64 * 9.85f64);
    let clay_scaled = (clay - (17.41f64 - 2.0f64 * 4.69f64)) / (4.0f64 * 4.69f64);
    let response_scaled =
        0.873f64 - 0.555f64 * bd_scaled - 0.399f64 * silt_scaled + 0.224f64 * clay_scaled;
    4.0f64 * 0.25f64 * response_scaled + 0.37f64 - 2.0f64 * 0.25f64
}
#[cfg(test)]
mod calc_ptf_zhao2016_m2_1_tests {
    use super::*;
    use crate::test_support::assert_close;
    #[test]
    fn published_predictor_means() {
        let result = calc_ptf_zhao2016_m2_1(1.32f64, 63.33f64, 17.41f64);
        assert_close(
            result,
            0.378f64,
            0.0001f64,
            0.01f64,
            "saturated_hydraulic_conductivity",
            "millimeter_per_minute",
            "registry",
        );
    }
    #[test]
    fn interior_soil() {
        let result = calc_ptf_zhao2016_m2_1(1.2f64, 60f64, 20f64);
        assert_close(
            result,
            0.561576533395387f64,
            0.0001f64,
            0.01f64,
            "saturated_hydraulic_conductivity",
            "millimeter_per_minute",
            "registry",
        );
    }
}
#[doc = r"Estimate saturated hydraulic conductivity using reconstructed M2-2.

# Arguments

  * bulk_density: Soil bulk density measured on undisturbed cores. (g/cm^3)
  * silt: Silt content measured by laser diffraction using USDA taxonomy (Section 2.2.2). (%)
  * clay: Clay content measured by laser diffraction using USDA taxonomy (Section 2.2.2). (%)

# Returns

  * ks: Saturated hydraulic conductivity, measured by the constant-head method. (mm/min)

# Notes

Prediction target: Saturated hydraulic conductivity
Mean coefficients of the top 25 percent, ranked by descending R-squared (Section 3.3).
Uses Table 1 means and standard deviations for Jia et al. (2012) Equation 3 on both predictors
and response, as a reviewed reconstruction.

# Warnings

The publication does not explicitly confirm Table 1 statistics as the fitted scaling constants
or the application of response scaling. This reconstruction has not been verified against author
software.
Observed ranges describe the sampled soils; predictions are not clamped."]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_zhao2016_m2_2(bulk_density: f64, silt: f64, clay: f64) -> f64 {
    let bd_scaled = (bulk_density - (1.32f64 - 2.0f64 * 0.14f64)) / (4.0f64 * 0.14f64);
    let silt_scaled = (silt - (63.33f64 - 2.0f64 * 9.85f64)) / (4.0f64 * 9.85f64);
    let clay_scaled = (clay - (17.41f64 - 2.0f64 * 4.69f64)) / (4.0f64 * 4.69f64);
    let response_scaled =
        0.868f64 - 0.557f64 * bd_scaled - 0.408f64 * silt_scaled + 0.231f64 * clay_scaled;
    4.0f64 * 0.25f64 * response_scaled + 0.37f64 - 2.0f64 * 0.25f64
}
#[cfg(test)]
mod calc_ptf_zhao2016_m2_2_tests {
    use super::*;
    use crate::test_support::assert_close;
    #[test]
    fn published_predictor_means() {
        let result = calc_ptf_zhao2016_m2_2(1.32f64, 63.33f64, 17.41f64);
        assert_close(
            result,
            0.371f64,
            0.0001f64,
            0.01f64,
            "saturated_hydraulic_conductivity",
            "millimeter_per_minute",
            "registry",
        );
    }
    #[test]
    fn interior_soil() {
        let result = calc_ptf_zhao2016_m2_2(1.2f64, 60f64, 20f64);
        assert_close(
            result,
            0.5567321826328834f64,
            0.0001f64,
            0.01f64,
            "saturated_hydraulic_conductivity",
            "millimeter_per_minute",
            "registry",
        );
    }
}
#[doc = r"Estimate saturated hydraulic conductivity using reconstructed M2-3.

# Arguments

  * bulk_density: Soil bulk density measured on undisturbed cores. (g/cm^3)
  * silt: Silt content measured by laser diffraction using USDA taxonomy (Section 2.2.2). (%)
  * clay: Clay content measured by laser diffraction using USDA taxonomy (Section 2.2.2). (%)

# Returns

  * ks: Saturated hydraulic conductivity, measured by the constant-head method. (mm/min)

# Notes

Prediction target: Saturated hydraulic conductivity
Mean coefficients of the top 50 percent, ranked by descending R-squared (Section 3.3).
Uses Table 1 means and standard deviations for Jia et al. (2012) Equation 3 on both predictors
and response, as a reviewed reconstruction.

# Warnings

The publication does not explicitly confirm Table 1 statistics as the fitted scaling constants
or the application of response scaling. This reconstruction has not been verified against author
software.
Observed ranges describe the sampled soils; predictions are not clamped."]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_zhao2016_m2_3(bulk_density: f64, silt: f64, clay: f64) -> f64 {
    let bd_scaled = (bulk_density - (1.32f64 - 2.0f64 * 0.14f64)) / (4.0f64 * 0.14f64);
    let silt_scaled = (silt - (63.33f64 - 2.0f64 * 9.85f64)) / (4.0f64 * 9.85f64);
    let clay_scaled = (clay - (17.41f64 - 2.0f64 * 4.69f64)) / (4.0f64 * 4.69f64);
    let response_scaled =
        0.872f64 - 0.565f64 * bd_scaled - 0.413f64 * silt_scaled + 0.236f64 * clay_scaled;
    4.0f64 * 0.25f64 * response_scaled + 0.37f64 - 2.0f64 * 0.25f64
}
#[cfg(test)]
mod calc_ptf_zhao2016_m2_3_tests {
    use super::*;
    use crate::test_support::assert_close;
    #[test]
    fn published_predictor_means() {
        let result = calc_ptf_zhao2016_m2_3(1.32f64, 63.33f64, 17.41f64);
        assert_close(
            result,
            0.371f64,
            0.0001f64,
            0.01f64,
            "saturated_hydraulic_conductivity",
            "millimeter_per_minute",
            "registry",
        );
    }
    #[test]
    fn interior_soil() {
        let result = calc_ptf_zhao2016_m2_3(1.2f64, 60f64, 20f64);
        assert_close(
            result,
            0.5595593556871192f64,
            0.0001f64,
            0.01f64,
            "saturated_hydraulic_conductivity",
            "millimeter_per_minute",
            "registry",
        );
    }
}
#[doc = r"Estimate saturated hydraulic conductivity using reconstructed M2-4.

# Arguments

  * bulk_density: Soil bulk density measured on undisturbed cores. (g/cm^3)
  * silt: Silt content measured by laser diffraction using USDA taxonomy (Section 2.2.2). (%)
  * clay: Clay content measured by laser diffraction using USDA taxonomy (Section 2.2.2). (%)

# Returns

  * ks: Saturated hydraulic conductivity, measured by the constant-head method. (mm/min)

# Notes

Prediction target: Saturated hydraulic conductivity
Mean coefficients of the top 75 percent, ranked by descending R-squared (Section 3.3).
Uses Table 1 means and standard deviations for Jia et al. (2012) Equation 3 on both predictors
and response, as a reviewed reconstruction.

# Warnings

The publication does not explicitly confirm Table 1 statistics as the fitted scaling constants
or the application of response scaling. This reconstruction has not been verified against author
software.
Observed ranges describe the sampled soils; predictions are not clamped."]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_zhao2016_m2_4(bulk_density: f64, silt: f64, clay: f64) -> f64 {
    let bd_scaled = (bulk_density - (1.32f64 - 2.0f64 * 0.14f64)) / (4.0f64 * 0.14f64);
    let silt_scaled = (silt - (63.33f64 - 2.0f64 * 9.85f64)) / (4.0f64 * 9.85f64);
    let clay_scaled = (clay - (17.41f64 - 2.0f64 * 4.69f64)) / (4.0f64 * 4.69f64);
    let response_scaled =
        0.877f64 - 0.572f64 * bd_scaled - 0.417f64 * silt_scaled + 0.237f64 * clay_scaled;
    4.0f64 * 0.25f64 * response_scaled + 0.37f64 - 2.0f64 * 0.25f64
}
#[cfg(test)]
mod calc_ptf_zhao2016_m2_4_tests {
    use super::*;
    use crate::test_support::assert_close;
    #[test]
    fn published_predictor_means() {
        let result = calc_ptf_zhao2016_m2_4(1.32f64, 63.33f64, 17.41f64);
        assert_close(
            result,
            0.371f64,
            0.0001f64,
            0.01f64,
            "saturated_hydraulic_conductivity",
            "millimeter_per_minute",
            "registry",
        );
    }
    #[test]
    fn interior_soil() {
        let result = calc_ptf_zhao2016_m2_4(1.2f64, 60f64, 20f64);
        assert_close(
            result,
            0.5615354864546015f64,
            0.0001f64,
            0.01f64,
            "saturated_hydraulic_conductivity",
            "millimeter_per_minute",
            "registry",
        );
    }
}
#[doc = r"Estimate saturated hydraulic conductivity using reconstructed M2-5.

# Arguments

  * bulk_density: Soil bulk density measured on undisturbed cores. (g/cm^3)
  * silt: Silt content measured by laser diffraction using USDA taxonomy (Section 2.2.2). (%)
  * clay: Clay content measured by laser diffraction using USDA taxonomy (Section 2.2.2). (%)

# Returns

  * ks: Saturated hydraulic conductivity, measured by the constant-head method. (mm/min)

# Notes

Prediction target: Saturated hydraulic conductivity
Mean coefficients of all 400 PTFs, ranked by descending R-squared (Section 3.3).
Uses Table 1 means and standard deviations for Jia et al. (2012) Equation 3 on both predictors
and response, as a reviewed reconstruction.

# Warnings

The publication does not explicitly confirm Table 1 statistics as the fitted scaling constants
or the application of response scaling. This reconstruction has not been verified against author
software.
Observed ranges describe the sampled soils; predictions are not clamped."]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_zhao2016_m2_5(bulk_density: f64, silt: f64, clay: f64) -> f64 {
    let bd_scaled = (bulk_density - (1.32f64 - 2.0f64 * 0.14f64)) / (4.0f64 * 0.14f64);
    let silt_scaled = (silt - (63.33f64 - 2.0f64 * 9.85f64)) / (4.0f64 * 9.85f64);
    let clay_scaled = (clay - (17.41f64 - 2.0f64 * 4.69f64)) / (4.0f64 * 4.69f64);
    let response_scaled =
        0.882f64 - 0.581f64 * bd_scaled - 0.422f64 * silt_scaled + 0.240f64 * clay_scaled;
    4.0f64 * 0.25f64 * response_scaled + 0.37f64 - 2.0f64 * 0.25f64
}
#[cfg(test)]
mod calc_ptf_zhao2016_m2_5_tests {
    use super::*;
    use crate::test_support::assert_close;
    #[test]
    fn published_predictor_means() {
        let result = calc_ptf_zhao2016_m2_5(1.32f64, 63.33f64, 17.41f64);
        assert_close(
            result,
            0.3705f64,
            0.0001f64,
            0.01f64,
            "saturated_hydraulic_conductivity",
            "millimeter_per_minute",
            "registry",
        );
    }
    #[test]
    fn interior_soil() {
        let result = calc_ptf_zhao2016_m2_5(1.2f64, 60f64, 20f64);
        assert_close(
            result,
            0.5638008258201379f64,
            0.0001f64,
            0.01f64,
            "saturated_hydraulic_conductivity",
            "millimeter_per_minute",
            "registry",
        );
    }
}
#[doc = r"Estimate saturated hydraulic conductivity using reconstructed M2-6.

# Arguments

  * bulk_density: Soil bulk density measured on undisturbed cores. (g/cm^3)
  * silt: Silt content measured by laser diffraction using USDA taxonomy (Section 2.2.2). (%)
  * clay: Clay content measured by laser diffraction using USDA taxonomy (Section 2.2.2). (%)

# Returns

  * ks: Saturated hydraulic conductivity, measured by the constant-head method. (mm/min)

# Notes

Prediction target: Saturated hydraulic conductivity
Lowest-ranked fitted PTF, ranked by descending R-squared (Section 3.3).
Uses Table 1 means and standard deviations for Jia et al. (2012) Equation 3 on both predictors
and response, as a reviewed reconstruction.

# Warnings

The publication does not explicitly confirm Table 1 statistics as the fitted scaling constants
or the application of response scaling. This reconstruction has not been verified against author
software.
Observed ranges describe the sampled soils; predictions are not clamped."]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_zhao2016_m2_6(bulk_density: f64, silt: f64, clay: f64) -> f64 {
    let bd_scaled = (bulk_density - (1.32f64 - 2.0f64 * 0.14f64)) / (4.0f64 * 0.14f64);
    let silt_scaled = (silt - (63.33f64 - 2.0f64 * 9.85f64)) / (4.0f64 * 9.85f64);
    let clay_scaled = (clay - (17.41f64 - 2.0f64 * 4.69f64)) / (4.0f64 * 4.69f64);
    let response_scaled =
        0.891f64 - 0.637f64 * bd_scaled - 0.470f64 * silt_scaled + 0.316f64 * clay_scaled;
    4.0f64 * 0.25f64 * response_scaled + 0.37f64 - 2.0f64 * 0.25f64
}
#[cfg(test)]
mod calc_ptf_zhao2016_m2_6_tests {
    use super::*;
    use crate::test_support::assert_close;
    #[test]
    fn published_predictor_means() {
        let result = calc_ptf_zhao2016_m2_6(1.32f64, 63.33f64, 17.41f64);
        assert_close(
            result,
            0.3655f64,
            0.0001f64,
            0.01f64,
            "saturated_hydraulic_conductivity",
            "millimeter_per_minute",
            "registry",
        );
    }
    #[test]
    fn interior_soil() {
        let result = calc_ptf_zhao2016_m2_6(1.2f64, 60f64, 20f64);
        assert_close(
            result,
            0.5853502159254489f64,
            0.0001f64,
            0.01f64,
            "saturated_hydraulic_conductivity",
            "millimeter_per_minute",
            "registry",
        );
    }
}
