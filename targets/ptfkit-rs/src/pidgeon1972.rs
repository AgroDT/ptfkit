// @generated by ptfkit-codegen; DO NOT EDIT.

#![doc = r"Pidgeon (1972), available-water regressions for ferrallitic soils in Uganda.

# Reference

Pidgeon, J. D. (1972). The measurement and prediction of available water capacity of ferrallitic
soils in Uganda. *Journal of Soil Science*, *23*(4), 431-441.

# Territory

Non-alluvial ferrallitic soils in Uganda, predominantly kaolinitic and possibly illitic

# Dataset

Ugandan profile samples; Entebbe alluvial and Nabbongo montmorillonitic soils were excluded from
the adopted regressions."]

#[doc = r"Estimate gravimetric field capacity from silt, clay, and organic matter.

# Arguments

  * silt: Silt measured by particle-size method 2. (%)
  * clay: Clay measured by particle-size method 2. (%)
  * organic_matter: Organic matter content. (%)

# Returns

  * field_capacity: Gravimetric field capacity. (% w/w)

# Models

  * h(theta): Point estimate

# Notes

Prediction target: Gravimetric field capacity
The reviewed organic-matter coefficient is 1.54."]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_pidgeon1972_fc(silt: f64, clay: f64, organic_matter: f64) -> f64 {
    7.38f64 + 0.16f64 * silt + 0.30f64 * clay + 1.54f64 * organic_matter
}
#[cfg(test)]
mod calc_ptf_pidgeon1972_fc_tests {
    use super::*;
    fn assert_in_interval(actual: f64, lower: f64, upper: f64) {
        assert!(
            actual >= lower && actual <= upper,
            "actual {actual} is outside [{lower}, {upper}]"
        );
    }
    #[test]
    fn regression_case() {
        let result = calc_ptf_pidgeon1972_fc(30f64, 20f64, 2f64);
        assert_in_interval(result, 21.259999999999973f64, 21.26000000000003f64);
    }
}
#[doc = r"Estimate gravimetric field capacity from sand.

# Arguments

  * sand: Sand measured by particle-size method 2. (%)

# Returns

  * field_capacity: Gravimetric field capacity. (% w/w)

# Models

  * h(theta): Point estimate

# Notes

Prediction target: Gravimetric field capacity"]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_pidgeon1972_fc_sand(sand: f64) -> f64 {
    36.16f64 - 0.25f64 * sand
}
#[cfg(test)]
mod calc_ptf_pidgeon1972_fc_sand_tests {
    use super::*;
    fn assert_in_interval(actual: f64, lower: f64, upper: f64) {
        assert!(
            actual >= lower && actual <= upper,
            "actual {actual} is outside [{lower}, {upper}]"
        );
    }
    #[test]
    fn regression_case() {
        let result = calc_ptf_pidgeon1972_fc_sand(50f64);
        assert_in_interval(result, 23.65999999999997f64, 23.66000000000003f64);
    }
}
#[doc = r"Estimate gravimetric field capacity from sand and organic matter.

# Arguments

  * sand: Sand measured by particle-size method 2. (%)
  * organic_matter: Organic matter content. (%)

# Returns

  * field_capacity: Gravimetric field capacity. (% w/w)

# Models

  * h(theta): Point estimate

# Notes

Prediction target: Gravimetric field capacity"]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_pidgeon1972_fc_sand_organic_matter(sand: f64, organic_matter: f64) -> f64 {
    34.27f64 - 0.27f64 * sand + 1.25f64 * organic_matter
}
#[cfg(test)]
mod calc_ptf_pidgeon1972_fc_sand_organic_matter_tests {
    use super::*;
    fn assert_in_interval(actual: f64, lower: f64, upper: f64) {
        assert!(
            actual >= lower && actual <= upper,
            "actual {actual} is outside [{lower}, {upper}]"
        );
    }
    #[test]
    fn regression_case() {
        let result = calc_ptf_pidgeon1972_fc_sand_organic_matter(50f64, 2f64);
        assert_in_interval(result, 23.26999999999997f64, 23.270000000000028f64);
    }
}
#[doc = r"Estimate volumetric field capacity from sand and organic matter.

# Arguments

  * sand: Sand measured by particle-size method 2. (%)
  * organic_matter: Organic matter content. (%)

# Returns

  * field_capacity: Volumetric field capacity. (% v/v)

# Models

  * h(theta): Point estimate

# Notes

Prediction target: Volumetric field capacity"]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_pidgeon1972_fc_vol_sand_organic_matter(sand: f64, organic_matter: f64) -> f64 {
    38.15f64 - 0.17f64 * sand + 0.77f64 * organic_matter
}
#[cfg(test)]
mod calc_ptf_pidgeon1972_fc_vol_sand_organic_matter_tests {
    use super::*;
    fn assert_in_interval(actual: f64, lower: f64, upper: f64) {
        assert!(
            actual >= lower && actual <= upper,
            "actual {actual} is outside [{lower}, {upper}]"
        );
    }
    #[test]
    fn regression_case() {
        let result = calc_ptf_pidgeon1972_fc_vol_sand_organic_matter(50f64, 2f64);
        assert_in_interval(result, 31.189999999999973f64, 31.19000000000003f64);
    }
}
#[doc = r"Estimate permanent wilting point from silt, clay, and organic matter.

# Arguments

  * silt: Silt measured by particle-size method 2. (%)
  * clay: Clay measured by particle-size method 2. (%)
  * organic_matter: Organic matter content. (%)

# Returns

  * permanent_wilting_point: Permanent wilting point. (% w/w)

# Models

  * h(theta): Point estimate

# Notes

Prediction target: Gravimetric permanent wilting point"]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_pidgeon1972_pwp(silt: f64, clay: f64, organic_matter: f64) -> f64 {
    -4.19f64 + 0.19f64 * silt + 0.39f64 * clay + 0.90f64 * organic_matter
}
#[cfg(test)]
mod calc_ptf_pidgeon1972_pwp_tests {
    use super::*;
    fn assert_in_interval(actual: f64, lower: f64, upper: f64) {
        assert!(
            actual >= lower && actual <= upper,
            "actual {actual} is outside [{lower}, {upper}]"
        );
    }
    #[test]
    fn regression_case() {
        let result = calc_ptf_pidgeon1972_pwp(30f64, 20f64, 2f64);
        assert_in_interval(result, 11.109999999999985f64, 11.110000000000014f64);
    }
}
#[doc = r"Estimate permanent wilting point from sand.

# Arguments

  * sand: Sand measured by particle-size method 1. (%)

# Returns

  * permanent_wilting_point: Permanent wilting point. (% w/w)

# Models

  * h(theta): Point estimate

# Notes

Prediction target: Gravimetric permanent wilting point"]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_pidgeon1972_pwp_sand(sand: f64) -> f64 {
    28.41f64 - 0.29f64 * sand
}
#[cfg(test)]
mod calc_ptf_pidgeon1972_pwp_sand_tests {
    use super::*;
    fn assert_in_interval(actual: f64, lower: f64, upper: f64) {
        assert!(
            actual >= lower && actual <= upper,
            "actual {actual} is outside [{lower}, {upper}]"
        );
    }
    #[test]
    fn regression_case() {
        let result = calc_ptf_pidgeon1972_pwp_sand(50f64);
        assert_in_interval(result, 13.909999999999986f64, 13.910000000000014f64);
    }
}
#[doc = r"Estimate permanent wilting point from sand and organic matter.

# Arguments

  * sand: Sand measured by particle-size method 2. (%)
  * organic_matter: Organic matter content. (%)

# Returns

  * permanent_wilting_point: Permanent wilting point. (% w/w)

# Models

  * h(theta): Point estimate

# Notes

Prediction target: Gravimetric permanent wilting point"]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_pidgeon1972_pwp_sand_organic_matter(sand: f64, organic_matter: f64) -> f64 {
    32.90f64 - 0.37f64 * sand + 0.44f64 * organic_matter
}
#[cfg(test)]
mod calc_ptf_pidgeon1972_pwp_sand_organic_matter_tests {
    use super::*;
    fn assert_in_interval(actual: f64, lower: f64, upper: f64) {
        assert!(
            actual >= lower && actual <= upper,
            "actual {actual} is outside [{lower}, {upper}]"
        );
    }
    #[test]
    fn regression_case() {
        let result = calc_ptf_pidgeon1972_pwp_sand_organic_matter(50f64, 2f64);
        assert_in_interval(result, 15.279999999999985f64, 15.280000000000014f64);
    }
}
#[doc = r"Estimate available water capacity from clay and organic matter.

# Arguments

  * clay: Clay measured by particle-size method 1. (%)
  * organic_matter: Organic matter content. (%)

# Returns

  * available_water_capacity: Available water capacity. (mm/m)

# Models

  * h(theta): Available-water estimate

# Notes

Prediction target: Available water capacity"]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_pidgeon1972_awc(clay: f64, organic_matter: f64) -> f64 {
    169.3f64 - 1.50f64 * clay + 6.09f64 * organic_matter
}
#[cfg(test)]
mod calc_ptf_pidgeon1972_awc_tests {
    use super::*;
    fn assert_in_interval(actual: f64, lower: f64, upper: f64) {
        assert!(
            actual >= lower && actual <= upper,
            "actual {actual} is outside [{lower}, {upper}]"
        );
    }
    #[test]
    fn regression_case() {
        let result = calc_ptf_pidgeon1972_awc(20f64, 2f64);
        assert_in_interval(result, 151.47999999999976f64, 151.48000000000022f64);
    }
}
#[doc = r"Estimate available water capacity from sand and organic matter.

# Arguments

  * sand: Sand measured by particle-size method 2. (%)
  * organic_matter: Organic matter content. (%)

# Returns

  * available_water_capacity: Available water capacity. (mm/m)

# Models

  * h(theta): Available-water estimate

# Notes

Prediction target: Available water capacity"]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_pidgeon1972_awc_sand_organic_matter(sand: f64, organic_matter: f64) -> f64 {
    1.0f64 + 1.84f64 * sand + 8.12f64 * organic_matter
}
#[cfg(test)]
mod calc_ptf_pidgeon1972_awc_sand_organic_matter_tests {
    use super::*;
    fn assert_in_interval(actual: f64, lower: f64, upper: f64) {
        assert!(
            actual >= lower && actual <= upper,
            "actual {actual} is outside [{lower}, {upper}]"
        );
    }
    #[test]
    fn regression_case() {
        let result = calc_ptf_pidgeon1972_awc_sand_organic_matter(50f64, 2f64);
        assert_in_interval(result, 109.23999999999988f64, 109.24000000000011f64);
    }
}
#[doc = r"Estimate available water capacity from coarse sand.

# Arguments

  * coarse_sand: Coarse sand measured by particle-size method 1. (%)

# Returns

  * available_water_capacity: Available water capacity. (mm/m)

# Models

  * h(theta): Available-water estimate

# Notes

Prediction target: Available water capacity"]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_pidgeon1972_awc_coarse_sand(coarse_sand: f64) -> f64 {
    68.5f64 + 2.33f64 * coarse_sand
}
#[cfg(test)]
mod calc_ptf_pidgeon1972_awc_coarse_sand_tests {
    use super::*;
    fn assert_in_interval(actual: f64, lower: f64, upper: f64) {
        assert!(
            actual >= lower && actual <= upper,
            "actual {actual} is outside [{lower}, {upper}]"
        );
    }
    #[test]
    fn regression_case() {
        let result = calc_ptf_pidgeon1972_awc_coarse_sand(20f64);
        assert_in_interval(result, 115.09999999999988f64, 115.10000000000011f64);
    }
}
#[doc = r"Estimate available water capacity from fine sand.

# Arguments

  * fine_sand: Fine sand measured by particle-size method 1. (%)

# Returns

  * available_water_capacity: Available water capacity. (mm/m)

# Models

  * h(theta): Available-water estimate

# Notes

Prediction target: Available water capacity"]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_pidgeon1972_awc_fine_sand(fine_sand: f64) -> f64 {
    66.7f64 + 2.66f64 * fine_sand
}
#[cfg(test)]
mod calc_ptf_pidgeon1972_awc_fine_sand_tests {
    use super::*;
    fn assert_in_interval(actual: f64, lower: f64, upper: f64) {
        assert!(
            actual >= lower && actual <= upper,
            "actual {actual} is outside [{lower}, {upper}]"
        );
    }
    #[test]
    fn regression_case() {
        let result = calc_ptf_pidgeon1972_awc_fine_sand(20f64);
        assert_in_interval(result, 119.89999999999989f64, 119.90000000000012f64);
    }
}
#[doc = r"Estimate available water capacity from very fine sand.

# Arguments

  * very_fine_sand: Very fine sand measured by particle-size method 1. (%)

# Returns

  * available_water_capacity: Available water capacity. (mm/m)

# Models

  * h(theta): Available-water estimate

# Notes

Prediction target: Available water capacity"]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_pidgeon1972_awc_very_fine_sand(very_fine_sand: f64) -> f64 {
    66.9f64 + 4.58f64 * very_fine_sand
}
#[cfg(test)]
mod calc_ptf_pidgeon1972_awc_very_fine_sand_tests {
    use super::*;
    fn assert_in_interval(actual: f64, lower: f64, upper: f64) {
        assert!(
            actual >= lower && actual <= upper,
            "actual {actual} is outside [{lower}, {upper}]"
        );
    }
    #[test]
    fn regression_case() {
        let result = calc_ptf_pidgeon1972_awc_very_fine_sand(10f64);
        assert_in_interval(result, 112.69999999999989f64, 112.70000000000012f64);
    }
}
#[doc = r"Estimate extended available water capacity from silt, clay, and organic matter.

# Arguments

  * silt: Silt measured by particle-size method 2. (%)
  * clay: Clay measured by particle-size method 2. (%)
  * organic_matter: Organic matter content. (%)

# Returns

  * extended_available_water_capacity: Extended available water capacity. (mm/m)

# Models

  * h(theta): Available-water estimate

# Notes

Prediction target: Extended available water capacity"]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_pidgeon1972_eawc(silt: f64, clay: f64, organic_matter: f64) -> f64 {
    121.1f64 - 3.03f64 * silt - 1.38f64 * clay + 6.76f64 * organic_matter
}
#[cfg(test)]
mod calc_ptf_pidgeon1972_eawc_tests {
    use super::*;
    fn assert_in_interval(actual: f64, lower: f64, upper: f64) {
        assert!(
            actual >= lower && actual <= upper,
            "actual {actual} is outside [{lower}, {upper}]"
        );
    }
    #[test]
    fn regression_case() {
        let result = calc_ptf_pidgeon1972_eawc(30f64, 20f64, 2f64);
        assert_in_interval(result, 16.119999999999973f64, 16.12000000000003f64);
    }
}
#[doc = r"Estimate extended available water capacity from sand.

# Arguments

  * sand: Sand measured by particle-size method 2. (%)

# Returns

  * extended_available_water_capacity: Extended available water capacity. (mm/m)

# Models

  * h(theta): Available-water estimate

# Notes

Prediction target: Extended available water capacity"]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_pidgeon1972_eawc_sand(sand: f64) -> f64 {
    -25.8f64 + 1.55f64 * sand
}
#[cfg(test)]
mod calc_ptf_pidgeon1972_eawc_sand_tests {
    use super::*;
    fn assert_in_interval(actual: f64, lower: f64, upper: f64) {
        assert!(
            actual >= lower && actual <= upper,
            "actual {actual} is outside [{lower}, {upper}]"
        );
    }
    #[test]
    fn regression_case() {
        let result = calc_ptf_pidgeon1972_eawc_sand(50f64);
        assert_in_interval(result, 51.699999999999946f64, 51.70000000000006f64);
    }
}
#[doc = r"Estimate extended available water capacity from sand and organic matter.

# Arguments

  * sand: Sand measured by particle-size method 1. (%)
  * organic_matter: Organic matter content. (%)

# Returns

  * extended_available_water_capacity: Extended available water capacity. (mm/m)

# Models

  * h(theta): Available-water estimate

# Notes

Prediction target: Extended available water capacity"]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_pidgeon1972_eawc_sand_organic_matter(sand: f64, organic_matter: f64) -> f64 {
    -10.8f64 + 1.15f64 * sand + 4.78f64 * organic_matter
}
#[cfg(test)]
mod calc_ptf_pidgeon1972_eawc_sand_organic_matter_tests {
    use super::*;
    fn assert_in_interval(actual: f64, lower: f64, upper: f64) {
        assert!(
            actual >= lower && actual <= upper,
            "actual {actual} is outside [{lower}, {upper}]"
        );
    }
    #[test]
    fn regression_case() {
        let result = calc_ptf_pidgeon1972_eawc_sand_organic_matter(50f64, 2f64);
        assert_in_interval(result, 56.25999999999994f64, 56.260000000000055f64);
    }
}
#[doc = r"Estimate extended available water capacity from coarse sand and organic matter.

# Arguments

  * coarse_sand: Coarse sand measured by particle-size method 1. (%)
  * organic_matter: Organic matter content. (%)

# Returns

  * extended_available_water_capacity: Extended available water capacity. (mm/m)

# Models

  * h(theta): Available-water estimate

# Notes

Prediction target: Extended available water capacity"]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_pidgeon1972_eawc_coarse_sand_organic_matter(
    coarse_sand: f64,
    organic_matter: f64,
) -> f64 {
    -7.4f64 + 2.37f64 * coarse_sand + 6.86f64 * organic_matter
}
#[cfg(test)]
mod calc_ptf_pidgeon1972_eawc_coarse_sand_organic_matter_tests {
    use super::*;
    fn assert_in_interval(actual: f64, lower: f64, upper: f64) {
        assert!(
            actual >= lower && actual <= upper,
            "actual {actual} is outside [{lower}, {upper}]"
        );
    }
    #[test]
    fn regression_case() {
        let result = calc_ptf_pidgeon1972_eawc_coarse_sand_organic_matter(20f64, 2f64);
        assert_in_interval(result, 53.71999999999994f64, 53.720000000000056f64);
    }
}
#[doc = r"Estimate extended available water capacity from fine sand and organic matter.

# Arguments

  * fine_sand: Fine sand measured by particle-size method 1. (%)
  * organic_matter: Organic matter content. (%)

# Returns

  * extended_available_water_capacity: Extended available water capacity. (mm/m)

# Models

  * h(theta): Available-water estimate

# Notes

Prediction target: Extended available water capacity"]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_pidgeon1972_eawc_fine_sand_organic_matter(
    fine_sand: f64,
    organic_matter: f64,
) -> f64 {
    -18.0f64 + 3.11f64 * fine_sand + 7.69f64 * organic_matter
}
#[cfg(test)]
mod calc_ptf_pidgeon1972_eawc_fine_sand_organic_matter_tests {
    use super::*;
    fn assert_in_interval(actual: f64, lower: f64, upper: f64) {
        assert!(
            actual >= lower && actual <= upper,
            "actual {actual} is outside [{lower}, {upper}]"
        );
    }
    #[test]
    fn regression_case() {
        let result = calc_ptf_pidgeon1972_eawc_fine_sand_organic_matter(20f64, 2f64);
        assert_in_interval(result, 59.57999999999994f64, 59.580000000000055f64);
    }
}
