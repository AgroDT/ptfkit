// @generated by ptfkit-codegen; DO NOT EDIT.

#![doc = r"Hodnett & Tomasella (2002), tropical-soil regressions for four van Genuchten parameters.

# Reference

Hodnett, M. G., & Tomasella, J. (2002). Marked differences between van Genuchten soil
water-retention parameters for temperate and tropical soils: A new water-retention pedo-transfer
functions developed for tropical soils. Geoderma, 108(3-4), 155-180.
DOI: 10.1016/S0016-7061(02)00105-2 (https://doi.org/10.1016/S0016-7061(02)00105-2)

# Territory

Tropical soils between approximately 25 degrees N and 25 degrees S.

# Dataset

The IGBP-DIS tropical-soil dataset contains 771 retained horizons from 249 profiles in 22
countries, split into 492 calibration curves and 279 validation curves."]

#[doc = r"Results returned by `calc_ptf_hodnett2002`."]
#[derive(Clone, Copy, Debug, PartialEq)]
pub struct Hodnett2002PTFResult {
    #[doc = r"Shape parameter of the van Genuchten water-retention model. (kPa^-1)"]
    pub alpha: f64,
    #[doc = r"Shape parameter controlling water-retention curve steepness. (dimensionless)"]
    pub n: f64,
    #[doc = r"Saturated volumetric soil water content. (m^3/m^3)"]
    pub theta_s: f64,
    #[doc = r"Residual volumetric soil water content. (m^3/m^3)"]
    pub theta_r: f64,
}
#[doc = r"Estimate four van Genuchten water-retention parameters for tropical soils.

# Arguments

  * sand: Sand content in the 0.05-2 mm USDA particle-size fraction, expressed as a percentage.
    (%)
  * silt: Silt content in the 0.002-0.05 mm USDA particle-size fraction, expressed as a
    percentage. (%)
  * clay: Clay content in the less-than-0.002 mm USDA particle-size fraction, expressed as a
    percentage. (%)
  * organic_carbon: Soil organic carbon content expressed as a percentage. (%)
  * bulk_density: Soil bulk density. (Mg/m^3)
  * cation_exchange_capacity: Soil cation exchange capacity. (cmol/kg)
  * ph: Soil pH. (dimensionless)

# Returns

A [`Hodnett2002PTFResult`].

# Models

  * h(theta): van Genuchten model with m = 1 - 1 / n

# Notes

Prediction target: Residual and saturated volumetric water content and the alpha and n shape
parameters of the van Genuchten water-retention model.
The regressions use percentages of sand, silt, clay, and organic carbon, bulk density in Mg/m^3,
CEC in cmol/kg, and pH.
Table 8 reports the reoptimised Step 3 coefficients used here and states that its displayed
values are multiplied by 100; each polynomial is therefore divided by 100 before alpha and n are
back-transformed.
The fitted ln(alpha) and ln(n) values are back-transformed with the natural exponential, as
specified by the paper's use of natural logarithms.

# Warnings

The paper reports n = 497 when introducing the continuous PTF, but its dataset split gives 492
calibration curves and 279 validation curves, which account for all 771 retained horizons. The
extraction treats 497 as a typographical error and records 492 as the calibration-set size.
The PTF did not reproduce water-retention curves accurately for soils with bulk density below
0.8 Mg/m^3, most of which were Andosols.
The PTF did not reproduce very low alpha values well because high-alpha soils dominated the
calibration data; mineralogy and structure were not directly represented."]
#[cfg_attr(feature = "inline", inline)]
#[must_use]
pub fn calc_ptf_hodnett2002(
    sand: f64,
    silt: f64,
    clay: f64,
    organic_carbon: f64,
    bulk_density: f64,
    cation_exchange_capacity: f64,
    ph: f64,
) -> Hodnett2002PTFResult {
    let clay_squared = clay.powi(2);
    let ln_alpha = (-2.294f64 - 3.526f64 * silt + 2.440f64 * organic_carbon
        - 0.076f64 * cation_exchange_capacity
        - 11.331f64 * ph
        + 0.019f64 * silt.powi(2))
        / 100.0f64;
    let alpha = ln_alpha.exp();
    let ln_n = (62.986f64 - 0.833f64 * clay - 0.529f64 * organic_carbon
        + 0.593f64 * ph
        + 0.0070f64 * clay_squared
        - 0.014f64 * sand * silt)
        / 100.0f64;
    let n = ln_n.exp();
    let theta_s = (81.799f64 + 0.099f64 * clay - 31.420f64 * bulk_density
        + 0.018f64 * cation_exchange_capacity
        + 0.451f64 * ph
        - 0.0005f64 * sand * clay)
        / 100.0f64;
    let theta_r = (22.733f64 - 0.164f64 * sand + 0.235f64 * cation_exchange_capacity
        - 0.831f64 * ph
        + 0.0018f64 * clay_squared
        + 0.0026f64 * sand * clay)
        / 100.0f64;
    Hodnett2002PTFResult {
        alpha,
        n,
        theta_s,
        theta_r,
    }
}
#[cfg(test)]
mod tests {
    use super::*;
    fn assert_close(actual: f64, expected: f64, atol: f64, rtol: f64) {
        assert!(
            (actual - expected).abs() <= atol + rtol * expected.abs(),
            "actual {actual} != expected {expected}"
        );
    }
    #[test]
    fn calibration_dataset_mean_properties() {
        let result =
            calc_ptf_hodnett2002(39.2f64, 24.2f64, 36.7f64, 1.4f64, 1.2f64, 15.9f64, 5.85f64);
        assert_close(
            result.alpha,
            0.245183615037873f64,
            0.000000000001f64,
            0.000000000001f64,
        );
        assert_close(
            result.n,
            1.36739326352383f64,
            0.000000000001f64,
            0.000000000001f64,
        );
        assert_close(
            result.theta_s,
            0.4993353f64,
            0.000000000001f64,
            0.000000000001f64,
        );
        assert_close(
            result.theta_r,
            0.21344216f64,
            0.000000000001f64,
            0.000000000001f64,
        );
    }
}
