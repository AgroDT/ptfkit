# @generated by ptfkit-codegen; DO NOT EDIT.
from __future__ import annotations

import pytest

from _helpers import assert_close, prepare_vector_case
from ptfkit.myeni2021 import calc_ptf_myeni2021_fc, calc_ptf_myeni2021_pwp


CASES_CALC_PTF_MYENI2021_FC = [
    ({'clay': 27.65, 'silt': 16.34, 'soc': 0.75}, {'fc': 0.19168}),
]
CASES_CALC_PTF_MYENI2021_FC_IDS = [
    'development_predictor_means',
]


@pytest.mark.parametrize(
    ('inputs', 'expected'), CASES_CALC_PTF_MYENI2021_FC, ids=CASES_CALC_PTF_MYENI2021_FC_IDS
)
def test_calc_ptf_myeni2021_fc_verification(inputs: dict[str, float], expected: dict[str, float]):
    result = calc_ptf_myeni2021_fc(**inputs)

    assert_close(
        result,
        expected['fc'],
        absolute=0.001,
        relative=0.0,
        quantity='gravimetric_water_content',
        unit='mass_fraction',
        source='registry',
    )


def test_calc_ptf_myeni2021_fc_array():
    inputs, expected, _out = prepare_vector_case(CASES_CALC_PTF_MYENI2021_FC)
    result = calc_ptf_myeni2021_fc(**inputs, out=None)
    assert_close(
        result[0],
        expected['fc'],
        absolute=0.001,
        relative=0.0,
        quantity='gravimetric_water_content',
        unit='mass_fraction',
        source='registry',
    )


def test_calc_ptf_myeni2021_fc_out():
    inputs, expected, out = prepare_vector_case(CASES_CALC_PTF_MYENI2021_FC)
    result = calc_ptf_myeni2021_fc(**inputs, out=out)
    assert result is out
    assert_close(
        result[0],
        expected['fc'],
        absolute=0.001,
        relative=0.0,
        quantity='gravimetric_water_content',
        unit='mass_fraction',
        source='registry',
    )


CASES_CALC_PTF_MYENI2021_PWP = [
    ({'clay': 27.65, 'silt': 16.34, 'soc': 0.75}, {'pwp': 0.10454}),
]
CASES_CALC_PTF_MYENI2021_PWP_IDS = [
    'development_predictor_means',
]


@pytest.mark.parametrize(
    ('inputs', 'expected'), CASES_CALC_PTF_MYENI2021_PWP, ids=CASES_CALC_PTF_MYENI2021_PWP_IDS
)
def test_calc_ptf_myeni2021_pwp_verification(inputs: dict[str, float], expected: dict[str, float]):
    result = calc_ptf_myeni2021_pwp(**inputs)

    assert_close(
        result,
        expected['pwp'],
        absolute=0.001,
        relative=0.0,
        quantity='gravimetric_water_content',
        unit='mass_fraction',
        source='registry',
    )


def test_calc_ptf_myeni2021_pwp_array():
    inputs, expected, _out = prepare_vector_case(CASES_CALC_PTF_MYENI2021_PWP)
    result = calc_ptf_myeni2021_pwp(**inputs, out=None)
    assert_close(
        result[0],
        expected['pwp'],
        absolute=0.001,
        relative=0.0,
        quantity='gravimetric_water_content',
        unit='mass_fraction',
        source='registry',
    )


def test_calc_ptf_myeni2021_pwp_out():
    inputs, expected, out = prepare_vector_case(CASES_CALC_PTF_MYENI2021_PWP)
    result = calc_ptf_myeni2021_pwp(**inputs, out=out)
    assert result is out
    assert_close(
        result[0],
        expected['pwp'],
        absolute=0.001,
        relative=0.0,
        quantity='gravimetric_water_content',
        unit='mass_fraction',
        source='registry',
    )
