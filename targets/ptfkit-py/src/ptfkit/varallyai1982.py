# @generated by ptfkit-codegen; DO NOT EDIT.

# ruff: noqa: I001

r"""Varallyai et al. (1982), water-retention parameter regressions for Hungarian soils.

Reference:
    Varallyai, D., Raikai, K., Mironenko, Ye. V., Pachepskiy, Ya. A., & Scherbakov, R. A. (1982). A
    mathematical description of basic water-physical soil characteristics. Pochvovedenie, (4),
    77-89.

Territory

:   Hungary, mainly the Hungarian Plain

Dataset

:   Undisturbed samples from 559 genetic horizons in 160 soil profiles; the meadow-series
    regressions used 68 samples and the chernozem comparison used 108 samples.

"""

from __future__ import annotations

from typing import TYPE_CHECKING, Generic, NamedTuple, TypeVar, overload

from ptfkit._ptfkit import (
    calc_ptf_varallyai1982_meadow as _calc_ptf_varallyai1982_meadow,
    calc_ptf_varallyai1982_chernozem_a as _calc_ptf_varallyai1982_chernozem_a,
    calc_ptf_varallyai1982_chernozem_b as _calc_ptf_varallyai1982_chernozem_b,
    calc_ptf_varallyai1982_chernozem_c as _calc_ptf_varallyai1982_chernozem_c,
)


if TYPE_CHECKING:
    from numpy import floating
    from numpy.typing import ArrayLike, NDArray

T = TypeVar('T')


class Varallyai1982Parameters(NamedTuple, Generic[T]):
    """Results returned by the matching PTF.

    Attributes:
        theta_0: Upper-asymptote water content parameter of equation (9). (vol.%)
        m: Shape parameter of equation (9). (dimensionless)
        pf_star: Base-10 logarithm of the equation (9) suction-scale parameter psi_*, with suction
            expressed in centimetres of water. (dimensionless)

    """

    theta_0: T
    m: T
    pf_star: T


__all__ = [
    'Varallyai1982Parameters',
    'calc_ptf_varallyai1982_chernozem_a',
    'calc_ptf_varallyai1982_chernozem_b',
    'calc_ptf_varallyai1982_chernozem_c',
    'calc_ptf_varallyai1982_meadow',
]


@overload
def calc_ptf_varallyai1982_meadow(
    *, bulk_density: float, fine_sand_fraction: float, fine_fraction: float
) -> Varallyai1982Parameters[floating]: ...


@overload
def calc_ptf_varallyai1982_meadow(
    *,
    bulk_density: ArrayLike,
    fine_sand_fraction: ArrayLike,
    fine_fraction: ArrayLike,
    out: Varallyai1982Parameters[NDArray[floating]] | None = None,
) -> Varallyai1982Parameters[NDArray[floating]]: ...


def calc_ptf_varallyai1982_meadow(
    *,
    bulk_density: float | ArrayLike,
    fine_sand_fraction: float | ArrayLike,
    fine_fraction: float | ArrayLike,
    out: Varallyai1982Parameters[NDArray[floating]] | None = None,
) -> Varallyai1982Parameters[floating] | Varallyai1982Parameters[NDArray[floating]]:
    r"""Estimate equation (9) water-retention parameters for meadow-series soils.

    Arguments:
        bulk_density: Undisturbed-soil bulk density. (g/cm^3)
        fine_sand_fraction: Mass fraction in the 0.25-0.05 mm particle-size class. (fraction)
        fine_fraction: Fine-particle mass fraction reported as particles smaller than 0.002 mm.
            (fraction)
        out: Optional output arrays for in-place calculation.

    Returns:
        Varallyai1982Parameters: Results grouped by result attributes.

    Territory:
        Hungary, mainly the Hungarian Plain

    Models:
        $h(\theta)$: theta = theta_0 / (1 + (psi / psi_*)^m)

    Notes:
        Prediction target: Parameters of the equation (9) water-retention model.
        Equation (14) was fitted to 68 meadow-series soil samples.
        S and C are fractions of one, while bulk density is in g/cm^3.
        The source reports 95% relative errors of 14% for theta_0, 34% for m, and 25% for pF_*.

    Warnings:
        Use is described as approximate by the source.
        The source does not report exact calibration ranges; avoid extrapolation beyond comparable
            Hungarian meadow-series soils.

    """
    if out is None:
        values = _calc_ptf_varallyai1982_meadow(bulk_density, fine_sand_fraction, fine_fraction)
    else:
        values = _calc_ptf_varallyai1982_meadow(
            bulk_density, fine_sand_fraction, fine_fraction, out=tuple(out)
        )

    return Varallyai1982Parameters(*values)


@overload
def calc_ptf_varallyai1982_chernozem_a(
    *, bulk_density: float, fine_fraction: float
) -> Varallyai1982Parameters[floating]: ...


@overload
def calc_ptf_varallyai1982_chernozem_a(
    *,
    bulk_density: ArrayLike,
    fine_fraction: ArrayLike,
    out: Varallyai1982Parameters[NDArray[floating]] | None = None,
) -> Varallyai1982Parameters[NDArray[floating]]: ...


def calc_ptf_varallyai1982_chernozem_a(
    *,
    bulk_density: float | ArrayLike,
    fine_fraction: float | ArrayLike,
    out: Varallyai1982Parameters[NDArray[floating]] | None = None,
) -> Varallyai1982Parameters[floating] | Varallyai1982Parameters[NDArray[floating]]:
    r"""Estimate equation (9) water-retention parameters for chernozem A horizons.

    Arguments:
        bulk_density: Undisturbed-soil bulk density. (g/cm^3)
        fine_fraction: Fine-particle mass fraction reported as particles smaller than 0.002 mm.
            (fraction)
        out: Optional output arrays for in-place calculation.

    Returns:
        Varallyai1982Parameters: Results grouped by result attributes.

    Territory:
        Hungary, mainly the Hungarian Plain

    Models:
        $h(\theta)$: theta = theta_0 / (1 + (psi / psi_*)^m)

    Notes:
        Prediction target: Parameters of the equation (9) water-retention model for chernozem A
            horizons.
        Equation (15) applies to chernozem A horizons.
        The source reports 95% relative errors of 10% for theta_0, 29% for m, and 18% for pF_*.

    Warnings:
        Use is described as approximate by the source.
        The source does not report exact calibration ranges; avoid extrapolation beyond comparable
            Hungarian chernozem A horizons.

    """
    if out is None:
        values = _calc_ptf_varallyai1982_chernozem_a(bulk_density, fine_fraction)
    else:
        values = _calc_ptf_varallyai1982_chernozem_a(bulk_density, fine_fraction, out=tuple(out))

    return Varallyai1982Parameters(*values)


@overload
def calc_ptf_varallyai1982_chernozem_b(
    *, bulk_density: float, fine_fraction: float
) -> Varallyai1982Parameters[floating]: ...


@overload
def calc_ptf_varallyai1982_chernozem_b(
    *,
    bulk_density: ArrayLike,
    fine_fraction: ArrayLike,
    out: Varallyai1982Parameters[NDArray[floating]] | None = None,
) -> Varallyai1982Parameters[NDArray[floating]]: ...


def calc_ptf_varallyai1982_chernozem_b(
    *,
    bulk_density: float | ArrayLike,
    fine_fraction: float | ArrayLike,
    out: Varallyai1982Parameters[NDArray[floating]] | None = None,
) -> Varallyai1982Parameters[floating] | Varallyai1982Parameters[NDArray[floating]]:
    r"""Estimate equation (9) water-retention parameters for chernozem B horizons.

    Arguments:
        bulk_density: Undisturbed-soil bulk density. (g/cm^3)
        fine_fraction: Fine-particle mass fraction reported as particles smaller than 0.002 mm.
            (fraction)
        out: Optional output arrays for in-place calculation.

    Returns:
        Varallyai1982Parameters: Results grouped by result attributes.

    Territory:
        Hungary, mainly the Hungarian Plain

    Models:
        $h(\theta)$: theta = theta_0 / (1 + (psi / psi_*)^m)

    Notes:
        Prediction target: Parameters of the equation (9) water-retention model for chernozem B
            horizons.
        Equation (16) applies to chernozem B horizons.
        The source reports 95% relative errors of 11% for theta_0, 34% for m, and 18% for pF_*.

    Warnings:
        Use is described as approximate by the source.
        The source does not report exact calibration ranges; avoid extrapolation beyond comparable
            Hungarian chernozem B horizons.

    """
    if out is None:
        values = _calc_ptf_varallyai1982_chernozem_b(bulk_density, fine_fraction)
    else:
        values = _calc_ptf_varallyai1982_chernozem_b(bulk_density, fine_fraction, out=tuple(out))

    return Varallyai1982Parameters(*values)


@overload
def calc_ptf_varallyai1982_chernozem_c(
    *, bulk_density: float, fine_fraction: float
) -> Varallyai1982Parameters[floating]: ...


@overload
def calc_ptf_varallyai1982_chernozem_c(
    *,
    bulk_density: ArrayLike,
    fine_fraction: ArrayLike,
    out: Varallyai1982Parameters[NDArray[floating]] | None = None,
) -> Varallyai1982Parameters[NDArray[floating]]: ...


def calc_ptf_varallyai1982_chernozem_c(
    *,
    bulk_density: float | ArrayLike,
    fine_fraction: float | ArrayLike,
    out: Varallyai1982Parameters[NDArray[floating]] | None = None,
) -> Varallyai1982Parameters[floating] | Varallyai1982Parameters[NDArray[floating]]:
    r"""Estimate equation (9) water-retention parameters for chernozem C horizons.

    Arguments:
        bulk_density: Undisturbed-soil bulk density. (g/cm^3)
        fine_fraction: Fine-particle mass fraction reported as particles smaller than 0.002 mm.
            (fraction)
        out: Optional output arrays for in-place calculation.

    Returns:
        Varallyai1982Parameters: Results grouped by result attributes.

    Territory:
        Hungary, mainly the Hungarian Plain

    Models:
        $h(\theta)$: theta = theta_0 / (1 + (psi / psi_*)^m)

    Notes:
        Prediction target: Parameters of the equation (9) water-retention model for chernozem C
            horizons.
        Equation (17) applies to chernozem C horizons.
        The source reports 95% relative errors of 7% for theta_0, 22% for m, and 11% for pF_*.

    Warnings:
        Use is described as approximate by the source.
        The source does not report exact calibration ranges; avoid extrapolation beyond comparable
            Hungarian chernozem C horizons.

    """
    if out is None:
        values = _calc_ptf_varallyai1982_chernozem_c(bulk_density, fine_fraction)
    else:
        values = _calc_ptf_varallyai1982_chernozem_c(bulk_density, fine_fraction, out=tuple(out))

    return Varallyai1982Parameters(*values)
