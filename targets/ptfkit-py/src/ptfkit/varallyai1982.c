/* @generated by ptfkit-codegen; DO NOT EDIT. */
#include <ptfkit/varallyai1982.h>
#include "ufunc.h"

static int calc_ptf_varallyai1982_meadow_contiguous_loop(PyArrayMethod_Context *context,
                                                         char *const *data,
                                                         const npy_intp *dimensions,
                                                         const npy_intp *strides,
                                                         NpyAuxData *transferdata) {
    (void)context;
    (void)strides;
    (void)transferdata;
    const double *in_bulk_density = (const double *)data[0];
    const double *in_fine_sand_fraction = (const double *)data[1];
    const double *in_fine_fraction = (const double *)data[2];
    double *out_theta_0 = (double *)data[3];
    double *out_m = (double *)data[4];
    double *out_pf_star = (double *)data[5];
    for (npy_intp index = 0; index < dimensions[0]; index++) {
        const double bulk_density = in_bulk_density[index];
        const double fine_sand_fraction = in_fine_sand_fraction[index];
        const double fine_fraction = in_fine_fraction[index];
        const varallyai1982_parameters ptfkit_result =
            calc_ptf_varallyai1982_meadow(bulk_density, fine_sand_fraction, fine_fraction);
        out_theta_0[index] = ptfkit_result.theta_0;
        out_m[index] = ptfkit_result.m;
        out_pf_star[index] = ptfkit_result.pf_star;
    }
    return 0;
}

static int calc_ptf_varallyai1982_meadow_strided_loop(PyArrayMethod_Context *context,
                                                      char *const *data, const npy_intp *dimensions,
                                                      const npy_intp *strides,
                                                      NpyAuxData *transferdata) {
    (void)context;
    (void)transferdata;
    for (npy_intp index = 0; index < dimensions[0]; index++) {
        const double bulk_density = *(const double *)(data[0] + index * strides[0]);
        const double fine_sand_fraction = *(const double *)(data[1] + index * strides[1]);
        const double fine_fraction = *(const double *)(data[2] + index * strides[2]);
        const varallyai1982_parameters ptfkit_result =
            calc_ptf_varallyai1982_meadow(bulk_density, fine_sand_fraction, fine_fraction);
        *(double *)(data[3] + index * strides[3]) = ptfkit_result.theta_0;
        *(double *)(data[4] + index * strides[4]) = ptfkit_result.m;
        *(double *)(data[5] + index * strides[5]) = ptfkit_result.pf_star;
    }
    return 0;
}
static PyType_Slot calc_ptf_varallyai1982_meadow_slots[] = {
    {NPY_METH_strided_loop, calc_ptf_varallyai1982_meadow_strided_loop},
    {NPY_METH_contiguous_loop, calc_ptf_varallyai1982_meadow_contiguous_loop},
    {0, NULL},
};
static PyArrayMethod_Spec calc_ptf_varallyai1982_meadow_spec = {
    .name = "calc_ptf_varallyai1982_meadow",
    .nin = 3,
    .nout = 3,
    .casting = NPY_SAME_KIND_CASTING,
    .slots = calc_ptf_varallyai1982_meadow_slots,
};

static int calc_ptf_varallyai1982_chernozem_a_contiguous_loop(PyArrayMethod_Context *context,
                                                              char *const *data,
                                                              const npy_intp *dimensions,
                                                              const npy_intp *strides,
                                                              NpyAuxData *transferdata) {
    (void)context;
    (void)strides;
    (void)transferdata;
    const double *in_bulk_density = (const double *)data[0];
    const double *in_fine_fraction = (const double *)data[1];
    double *out_theta_0 = (double *)data[2];
    double *out_m = (double *)data[3];
    double *out_pf_star = (double *)data[4];
    for (npy_intp index = 0; index < dimensions[0]; index++) {
        const double bulk_density = in_bulk_density[index];
        const double fine_fraction = in_fine_fraction[index];
        const varallyai1982_parameters ptfkit_result =
            calc_ptf_varallyai1982_chernozem_a(bulk_density, fine_fraction);
        out_theta_0[index] = ptfkit_result.theta_0;
        out_m[index] = ptfkit_result.m;
        out_pf_star[index] = ptfkit_result.pf_star;
    }
    return 0;
}

static int calc_ptf_varallyai1982_chernozem_a_strided_loop(PyArrayMethod_Context *context,
                                                           char *const *data,
                                                           const npy_intp *dimensions,
                                                           const npy_intp *strides,
                                                           NpyAuxData *transferdata) {
    (void)context;
    (void)transferdata;
    for (npy_intp index = 0; index < dimensions[0]; index++) {
        const double bulk_density = *(const double *)(data[0] + index * strides[0]);
        const double fine_fraction = *(const double *)(data[1] + index * strides[1]);
        const varallyai1982_parameters ptfkit_result =
            calc_ptf_varallyai1982_chernozem_a(bulk_density, fine_fraction);
        *(double *)(data[2] + index * strides[2]) = ptfkit_result.theta_0;
        *(double *)(data[3] + index * strides[3]) = ptfkit_result.m;
        *(double *)(data[4] + index * strides[4]) = ptfkit_result.pf_star;
    }
    return 0;
}
static PyType_Slot calc_ptf_varallyai1982_chernozem_a_slots[] = {
    {NPY_METH_strided_loop, calc_ptf_varallyai1982_chernozem_a_strided_loop},
    {NPY_METH_contiguous_loop, calc_ptf_varallyai1982_chernozem_a_contiguous_loop},
    {0, NULL},
};
static PyArrayMethod_Spec calc_ptf_varallyai1982_chernozem_a_spec = {
    .name = "calc_ptf_varallyai1982_chernozem_a",
    .nin = 2,
    .nout = 3,
    .casting = NPY_SAME_KIND_CASTING,
    .slots = calc_ptf_varallyai1982_chernozem_a_slots,
};

static int calc_ptf_varallyai1982_chernozem_b_contiguous_loop(PyArrayMethod_Context *context,
                                                              char *const *data,
                                                              const npy_intp *dimensions,
                                                              const npy_intp *strides,
                                                              NpyAuxData *transferdata) {
    (void)context;
    (void)strides;
    (void)transferdata;
    const double *in_bulk_density = (const double *)data[0];
    const double *in_fine_fraction = (const double *)data[1];
    double *out_theta_0 = (double *)data[2];
    double *out_m = (double *)data[3];
    double *out_pf_star = (double *)data[4];
    for (npy_intp index = 0; index < dimensions[0]; index++) {
        const double bulk_density = in_bulk_density[index];
        const double fine_fraction = in_fine_fraction[index];
        const varallyai1982_parameters ptfkit_result =
            calc_ptf_varallyai1982_chernozem_b(bulk_density, fine_fraction);
        out_theta_0[index] = ptfkit_result.theta_0;
        out_m[index] = ptfkit_result.m;
        out_pf_star[index] = ptfkit_result.pf_star;
    }
    return 0;
}

static int calc_ptf_varallyai1982_chernozem_b_strided_loop(PyArrayMethod_Context *context,
                                                           char *const *data,
                                                           const npy_intp *dimensions,
                                                           const npy_intp *strides,
                                                           NpyAuxData *transferdata) {
    (void)context;
    (void)transferdata;
    for (npy_intp index = 0; index < dimensions[0]; index++) {
        const double bulk_density = *(const double *)(data[0] + index * strides[0]);
        const double fine_fraction = *(const double *)(data[1] + index * strides[1]);
        const varallyai1982_parameters ptfkit_result =
            calc_ptf_varallyai1982_chernozem_b(bulk_density, fine_fraction);
        *(double *)(data[2] + index * strides[2]) = ptfkit_result.theta_0;
        *(double *)(data[3] + index * strides[3]) = ptfkit_result.m;
        *(double *)(data[4] + index * strides[4]) = ptfkit_result.pf_star;
    }
    return 0;
}
static PyType_Slot calc_ptf_varallyai1982_chernozem_b_slots[] = {
    {NPY_METH_strided_loop, calc_ptf_varallyai1982_chernozem_b_strided_loop},
    {NPY_METH_contiguous_loop, calc_ptf_varallyai1982_chernozem_b_contiguous_loop},
    {0, NULL},
};
static PyArrayMethod_Spec calc_ptf_varallyai1982_chernozem_b_spec = {
    .name = "calc_ptf_varallyai1982_chernozem_b",
    .nin = 2,
    .nout = 3,
    .casting = NPY_SAME_KIND_CASTING,
    .slots = calc_ptf_varallyai1982_chernozem_b_slots,
};

static int calc_ptf_varallyai1982_chernozem_c_contiguous_loop(PyArrayMethod_Context *context,
                                                              char *const *data,
                                                              const npy_intp *dimensions,
                                                              const npy_intp *strides,
                                                              NpyAuxData *transferdata) {
    (void)context;
    (void)strides;
    (void)transferdata;
    const double *in_bulk_density = (const double *)data[0];
    const double *in_fine_fraction = (const double *)data[1];
    double *out_theta_0 = (double *)data[2];
    double *out_m = (double *)data[3];
    double *out_pf_star = (double *)data[4];
    for (npy_intp index = 0; index < dimensions[0]; index++) {
        const double bulk_density = in_bulk_density[index];
        const double fine_fraction = in_fine_fraction[index];
        const varallyai1982_parameters ptfkit_result =
            calc_ptf_varallyai1982_chernozem_c(bulk_density, fine_fraction);
        out_theta_0[index] = ptfkit_result.theta_0;
        out_m[index] = ptfkit_result.m;
        out_pf_star[index] = ptfkit_result.pf_star;
    }
    return 0;
}

static int calc_ptf_varallyai1982_chernozem_c_strided_loop(PyArrayMethod_Context *context,
                                                           char *const *data,
                                                           const npy_intp *dimensions,
                                                           const npy_intp *strides,
                                                           NpyAuxData *transferdata) {
    (void)context;
    (void)transferdata;
    for (npy_intp index = 0; index < dimensions[0]; index++) {
        const double bulk_density = *(const double *)(data[0] + index * strides[0]);
        const double fine_fraction = *(const double *)(data[1] + index * strides[1]);
        const varallyai1982_parameters ptfkit_result =
            calc_ptf_varallyai1982_chernozem_c(bulk_density, fine_fraction);
        *(double *)(data[2] + index * strides[2]) = ptfkit_result.theta_0;
        *(double *)(data[3] + index * strides[3]) = ptfkit_result.m;
        *(double *)(data[4] + index * strides[4]) = ptfkit_result.pf_star;
    }
    return 0;
}
static PyType_Slot calc_ptf_varallyai1982_chernozem_c_slots[] = {
    {NPY_METH_strided_loop, calc_ptf_varallyai1982_chernozem_c_strided_loop},
    {NPY_METH_contiguous_loop, calc_ptf_varallyai1982_chernozem_c_contiguous_loop},
    {0, NULL},
};
static PyArrayMethod_Spec calc_ptf_varallyai1982_chernozem_c_spec = {
    .name = "calc_ptf_varallyai1982_chernozem_c",
    .nin = 2,
    .nout = 3,
    .casting = NPY_SAME_KIND_CASTING,
    .slots = calc_ptf_varallyai1982_chernozem_c_slots,
};

int ptfkit_register_varallyai1982(PyObject *module) {
    if (ptfkit_add_ufunc(module, "calc_ptf_varallyai1982_meadow", 3, 3,
                         &calc_ptf_varallyai1982_meadow_spec) < 0)
        return -1;
    if (ptfkit_add_ufunc(module, "calc_ptf_varallyai1982_chernozem_a", 2, 3,
                         &calc_ptf_varallyai1982_chernozem_a_spec) < 0)
        return -1;
    if (ptfkit_add_ufunc(module, "calc_ptf_varallyai1982_chernozem_b", 2, 3,
                         &calc_ptf_varallyai1982_chernozem_b_spec) < 0)
        return -1;
    if (ptfkit_add_ufunc(module, "calc_ptf_varallyai1982_chernozem_c", 2, 3,
                         &calc_ptf_varallyai1982_chernozem_c_spec) < 0)
        return -1;
    return 0;
}
