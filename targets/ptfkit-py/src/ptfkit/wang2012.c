/* @generated by ptfkit-codegen; DO NOT EDIT. */
#include "ufunc.h"

static void calc_ptf_wang2012_loop(char **args, const npy_intp *dimensions, const npy_intp *steps, void *data) {
    npy_intp index;
    for (index = 0; index < dimensions[0]; index++) {
        const double sand = *(const double *)args[0];
        const double silt = *(const double *)args[1];
        const double clay = *(const double *)args[2];
        const double bulk_density = *(const double *)args[3];
        const double soil_organic_carbon = *(const double *)args[4];
        const double altitude = *(const double *)args[5];
        const double soil_organic_carbon_g_per_kg = 10.0 * soil_organic_carbon;
        const double log10_k_sat_cm_per_day = 1.173 + 0.038 * silt + 0.690 * log10(sand) + 0.865 / sand - 0.030 * bulk_density * silt - 0.00000995 * soil_organic_carbon_g_per_kg * altitude;
        const double k_sat_cm_per_day = pow(10.0, log10_k_sat_cm_per_day);
        const double fc_percent = 46.481 - 4.757 * soil_organic_carbon_g_per_kg - 14.028 * log10(clay) - 13.991 * log10(sand) + 42.261 * log10(soil_organic_carbon_g_per_kg) - 11.763 / sand + 19.198 / soil_organic_carbon_g_per_kg - 5.448 * pow(bulk_density, 2.0) + 0.044 * pow(soil_organic_carbon_g_per_kg, 2.0) + 1.975 * bulk_density * soil_organic_carbon_g_per_kg;
        const double sswc_percent = 98.813 - 21.555 / bulk_density - 39.735 / silt - 2.091 / sand + 3.247 / soil_organic_carbon_g_per_kg - 17.096 * pow(bulk_density, 2.0);
        const double theta_s = sswc_percent / 100.0;
        const double theta_fc = fc_percent / 100.0;
        const double k_sat = k_sat_cm_per_day / 8640000.0;
        *(double *)args[6] = theta_s;
        *(double *)args[7] = theta_fc;
        *(double *)args[8] = k_sat;
        for (int arg = 0; arg < 9; arg++) args[arg] += steps[arg];
    }
}
static PyUFuncGenericFunction calc_ptf_wang2012_functions[] = { calc_ptf_wang2012_loop };
static char calc_ptf_wang2012_types[] = { NPY_DOUBLE, NPY_DOUBLE, NPY_DOUBLE, NPY_DOUBLE, NPY_DOUBLE, NPY_DOUBLE, NPY_DOUBLE, NPY_DOUBLE, NPY_DOUBLE };

int ptfkit_register_wang2012(PyObject *module) {
    if (ptfkit_add_ufunc(module, "calc_ptf_wang2012", calc_ptf_wang2012_functions, calc_ptf_wang2012_types, 6, 3) < 0) return -1;
    return 0;
}
