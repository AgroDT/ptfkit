/* @generated by ptfkit-codegen; DO NOT EDIT. */


#ifndef PTFKIT_RECORD_LOOKUP_EXPRESSION_H
#define PTFKIT_RECORD_LOOKUP_EXPRESSION_H

#include <math.h>

/**
 * @brief Test source.
 *
 * @details Source publication:
 * Test (2026).
 */

typedef enum {
    record_lookup_expression_texture_sand,
    record_lookup_expression_texture_clay,
} record_lookup_expression_texture;

typedef struct {
    double factor;
} parameters;

static inline parameters parameters_from_texture(record_lookup_expression_texture value) {
    static const parameters table[] = {
        {2.0},
        {3.0},
    };
    const unsigned index = (unsigned)value;
    if (index < sizeof(table) / sizeof(table[0])) {
        return table[index];
    }
    const parameters fallback = {NAN};
    return fallback;
}

/**
 * @brief Test value.
 * @param texture
 * @param x Test input. (1)
 * @return Test output. (1)
 *
 * @details Prediction target:
 * Test value.
 */
static inline double calc_ptf_record_lookup_expression(record_lookup_expression_texture texture, double x) {
    const parameters parameters = parameters_from_texture(texture);
    return parameters.factor * x;
}


#endif
