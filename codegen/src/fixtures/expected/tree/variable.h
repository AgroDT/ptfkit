/* @generated by ptfkit-codegen; DO NOT EDIT. */


#ifndef PTFKIT_TREE_VARIABLE_H
#define PTFKIT_TREE_VARIABLE_H

#include <math.h>

/**
 * @brief Test source.
 *
 * @details Source publication:
 * Test (2026).
 */

typedef enum {
    tree_variable_category_coarse,
    tree_variable_category_fine,
} tree_variable_category;

static inline double tree_variable_scalar_tree(tree_variable_category category, double predictor) {
    if (category == tree_variable_category_coarse) {
        if (predictor < 2.0) {
            return 1.0;
        } else {
            return 2.0;
        }
    } else {
        return 3.0;
    }
}

/**
 * @brief Test named tree.
 * @param category
 * @param x Test input. (1)
 * @return Test output. (1)
 *
 * @details Prediction target:
 * Test value.
 */
static inline double calc_ptf_tree_branch(tree_variable_category category, double x) {
    const double branch_value = tree_variable_scalar_tree(category, x);
    return branch_value * 10.0;
}


#endif
