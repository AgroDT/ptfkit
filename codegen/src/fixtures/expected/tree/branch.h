/* @generated by ptfkit-codegen; DO NOT EDIT. */


#ifndef PTFKIT_TREE_BRANCH_H
#define PTFKIT_TREE_BRANCH_H

#include <math.h>

/**
 * @brief Test source.
 *
 * @details Source publication:
 * Test (2026).
 */

typedef enum {
    tree_branch_category_coarse,
    tree_branch_category_fine,
} tree_branch_category;

static inline double tree_branch_scalar_tree(tree_branch_category category, double predictor) {
    if (category == tree_branch_category_coarse) {
        if (predictor < 2.0) {
            return 1.0;
        } else {
            return 2.0;
        }
    } else {
        return 3.0;
    }
}

/**
 * @brief Test named tree.
 * @param category
 * @param x Test input. (1)
 * @return Test output. (1)
 *
 * @details Prediction target:
 * Test value.
 */
static inline double calc_ptf_tree_branch(tree_branch_category category, double x) {
    return tree_branch_scalar_tree(category, x);
}


#endif
